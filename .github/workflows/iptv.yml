name: Process IPTV Sources

on:
  schedule:
    - cron: '0 */6 * * *'  # 每6小时运行一次
  workflow_dispatch:

jobs:
  process:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0      # 拉取完整历史，便于 rebase

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install requests

      - name: Run IPTV processor
        run: python zubo_iptv_processor.py

      - name: Commit results
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "41898282+github-actions[bot]@users.noreply.github.com"
          
          # 拉取远程最新代码
          git pull --rebase origin main
          
          # 添加更改
          git add zubo_iptv_sorted.m3u
          
          # 检查是否有变更
          if ! git diff --staged --quiet; then
            git commit -m "Auto-update stable IPTV sources $(date +'%Y-%m-%d %H:%M:%S')"
            # 再次拉取确保是最新
            git pull --rebase origin main
            git push origin main
            echo "✅ 已推送更新"
          else
            echo "📭 没有检测到文件变更，跳过提交"
          fi
